1.Create an HTML file with various elements demonstrating the use of attributes and include a link with attributes, and an image with attributes

2.Create an HTML document with a paragraph, a division containing multiple paragraphs, and a spann element within a paragraph.

3.Create an HTML document with all the kinds of lists available.

4.Create an HTML document that uses global attributes like class, id ,title ,style , tab index.